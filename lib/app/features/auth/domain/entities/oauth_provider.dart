class OAuthProvider {
  const OAuthProvider({
    required this.name,
    required this.displayName,
    required this.icon,
  });

  final String name;
  final String displayName;
  final String icon;
}
